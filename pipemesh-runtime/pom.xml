<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.pipemesh</groupId>
        <artifactId>pipemesh</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>pipemesh-runtime</artifactId>
    <name>PipeMesh Runtime</name>
    <description>
        The runnable runtime: a configuration directory in, a gRPC server out. An application
        that composes the library modules — never something they depend on.
    </description>

    <dependencies>
        <dependency>
            <groupId>io.pipemesh</groupId>
            <artifactId>pipemesh-grpc</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.pipemesh</groupId>
            <artifactId>pipemesh-postgres</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.pipemesh</groupId>
            <artifactId>pipemesh-openai-compatible</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.pipemesh</groupId>
            <artifactId>pipemesh-mcp</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <version>2.0.16</version>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!--
              A jar plus a lib directory rather than one fat jar. Two reasons:
              a container layer of dependencies changes far less often than the
              application, and nothing has to merge META-INF/services — gRPC
              finds its transports through those files, and a merge that drops
              one turns into a puzzling "no provider" at startup.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals><goal>copy-dependencies</goal></goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/lib</outputDirectory>
                            <includeScope>runtime</includeScope>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>io.pipemesh.runtime.RuntimeMain</mainClass>
                            <addClasspath>true</addClasspath>
                            <classpathPrefix>lib/</classpathPrefix>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>

<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.pipemesh</groupId>
        <artifactId>pipemesh</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>pipemesh-console</artifactId>
    <name>PipeMesh Console</name>
    <description>
        Sign-up, subscriptions and the web console. An application that uses PipeMesh — never
        something PipeMesh uses. Nothing in the runtime modules may depend on this one.
    </description>

    <dependencies>
        <dependency>
            <groupId>io.pipemesh</groupId>
            <artifactId>pipemesh-postgres</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!--
          For PrincipalResolver: the console is the application that finally
          answers "who is calling", which the runtime cannot answer for itself.
        -->
        <dependency>
            <groupId>io.pipemesh</groupId>
            <artifactId>pipemesh-grpc</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-crypto</artifactId>
        </dependency>
        <!-- Argon2 needs a provider; spring-security-crypto declares it optional. -->
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcprov-jdk18on</artifactId>
            <version>1.78</version>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!--
              Spring reads parameter names to bind @PathVariable and friends, and
              javac drops them unless asked. The runtime modules do not need this;
              this module is not inheriting spring-boot-starter-parent, which is
              where the flag would otherwise have come from.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <compilerArgs>
                        <arg>-parameters</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
